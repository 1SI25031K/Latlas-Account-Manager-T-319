import { AccountPageReveal } from "@/components/AccountPageReveal";

export default function DataPrivacyPage() {
  return (
    <AccountPageReveal className="mx-auto max-w-2xl space-y-4">
      <h1 className="font-heading-ja text-xl font-semibold" style={{ color: "var(--dashboard-text)" }}>
        データとプライバシー
      </h1>
      <p className="text-sm" style={{ color: "var(--dashboard-text-muted)" }}>
        このセクションは準備中です。
      </p>
    </AccountPageReveal>
  );
}
