/**
 * ホームに表示する「よくある質問」チップ用テキスト。
 *
 * 運用方針:
 * - 文言はこの配列に追加する。空のときはチップ UI を表示しない。
 * - 表示時はクライアントでシャッフルし（例: Fisher–Yates）、必要なら先頭 N 件だけ使う（現在は最大 3 件）。
 * - チップクリックでその文面をチャットのユーザーメッセージとして送信する。
 */
export const ACCOUNT_FAQ_ITEMS: string[] = [];

/** チップとして表示する最大件数 */
export const ACCOUNT_FAQ_DISPLAY_MAX = 3;
